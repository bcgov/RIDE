name: RIDE Failover


on:
  workflow_dispatch:

jobs:
  update-openshift:
    runs-on: ubuntu-latest
    name: Test
    environment:
      name: ${{ github.event.inputs.environment }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v6 
