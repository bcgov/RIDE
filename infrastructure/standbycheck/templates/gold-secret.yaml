{{- if and .Values.controller.enabled (not .Release.IsUpgrade) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "app.fullname" . }}
  labels: {{ include "app.labels" . | nindent 4 }}
type: Opaque
stringData:
  NAMESPACE: 
  SERVICEACCOUNT_TOKEN:
  STANDBY_OCP_API_SERVER:
  STANDBY_POSTGRESCLUSTER_NAME:
{{- end }}